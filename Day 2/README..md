# Day 2 – Data Types, Operators & Tip Calculator 🧮

Today I learned about Python’s primitive data types and how numbers and strings behave differently in programs.

## What I Learned

### Primitive Data Types
- **String (`str`)** – Text values
- **Integer (`int`)** – Whole numbers
- **Float (`float`)** – Decimal numbers
- **Boolean (`bool`)** – True or False values

I practiced:
- String indexing (positive & negative indexing)
- Subscripting strings (e.g. `"Hello"[0]`)
- Combining strings vs adding numbers
- Understanding TypeError and why data types matter

---

### Type Checking & Type Conversion

I learned how to:
- Check a data type using `type()`
- Convert between types using:
  - `int()`
  - `float()`
  - `str()`

Example:
```python
print(type("Hello"))  # <class 'str'>
print(type(123))      # <class 'int'>